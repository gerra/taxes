# Dev-only overlay (git-ignored, never deployed). Copy to secrets/.env.local.
# Loaded after .env with override=True — put only the keys that differ locally.

BASE_URL=http://localhost:5002
TAXES_DATA_DIR=./data
